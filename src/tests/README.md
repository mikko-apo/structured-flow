# Test structure

- each function:
  - should have its own file branch.test.ts
  - should be tested through every supported syntax
  - should be tested from initialization to every possible outcome
  - outcomes
    - each StepStatus